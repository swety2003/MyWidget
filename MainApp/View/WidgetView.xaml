<Page
    x:Class="MainApp.View.WidgetView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MainApp.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pu="clr-namespace:Panuon.WPF.UI;assembly=Panuon.WPF.UI"
    xmlns:ui="clr-namespace:PluginSDK.Controls;assembly=PluginSDK"
    xmlns:viewmodel="clr-namespace:MainApp.ViewModel"
    Title="WidgetView"
    d:DataContext="{d:DesignInstance Type=viewmodel:WidgetViewVM}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>

        <Canvas x:Name="cv">

            <TextBlock FontFamily="Century Gothic" />
            <!--<ItemsControl ItemsSource="{Binding ActiveCards}" />-->
            <!--<ui:MyThumb Background="Blue">
                <ui:MyThumb.Content>
                    <Border Padding="10">
                        <Button />
                    </Border>
                </ui:MyThumb.Content>
            </ui:MyThumb>

            <ui:MyThumb Background="Red" HeightPix="2" />-->

        </Canvas>
    </Grid>
</Page>
